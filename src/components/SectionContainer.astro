---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  class={`${className} w-full mx-auto max-w-[920px] py-8 sm:py-12 md:py-14 lg:py-16 animate-fade-up animate-once`}
>
  <slot />
</section>
